import React from "react"
import {Page, Upload} from "../components"

const UploadPage: React.FC = () => (
	<Page title="Share end-to-end encrypted files which expire automatically">
		<Upload />
	</Page>
)

export default UploadPage
