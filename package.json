{
  "name": "tarnhelm",
  "license": "MIT",
  "author": "bkiac <benceknab.iac@gmail.com>",
  "repository": "git@github.com:bkiac/tarnhelm.git",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "format": "yarn prettier && yarn lint",
    "lint": "lerna run lint --concurrency 1 --no-bail --stream",
    "test": "lerna run test --concurrency 1 --no-bail --stream",
    "prettier": "prettier-package-json --write && lerna run prettier"
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "prettier": "^2.1.1",
    "prettier-package-json": "^2.1.3"
  }
}
