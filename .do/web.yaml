name: tarnhelm-web
region: nyc
services:
  - name: web
    github:
      branch: master
      deploy_on_push: true
      repo: bkiac/tarnhelm
    source_dir: /packages/web
    environment_slug: node-js
    build_command: yarn build
